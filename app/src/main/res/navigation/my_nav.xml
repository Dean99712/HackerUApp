<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_nav"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.hackeruapp.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <fragment
        android:id="@+id/updateFragment"
        android:name="com.example.hackeruapp.UpdateFragment"
        android:label="fragment_update"
        tools:layout="@layout/fragment_person_update" >
        <action
            android:id="@+id/action_updateFragment_to_mainActivity"
            app:destination="@id/mainActivity" />
        <action
            android:id="@+id/action_updateFragment_to_mainActivity2"
            app:destination="@id/mainActivity" />
    </fragment>
</navigation>